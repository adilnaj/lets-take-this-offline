---
phase: 08-resolve-tech-debt
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-foundation/01-05-SUMMARY.md
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "AUTH-03 is marked [ ] (unchecked) in REQUIREMENTS.md — not [x]"
    - "A deferral note explaining the Apple OAuth status is present in REQUIREMENTS.md near AUTH-03"
    - "01-05-SUMMARY.md requirements-completed list no longer includes AUTH-03"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected AUTH-03 checkbox and inline deferral note"
      contains: "[ ] **AUTH-03**"
    - path: ".planning/phases/01-foundation/01-05-SUMMARY.md"
      provides: "Accurate requirements-completed list (no AUTH-03)"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "AUTH-03 checkbox"
      via: "[ ] marker + inline deferral comment"
      pattern: "\\[ \\] \\*\\*AUTH-03\\*\\*"
---

<objective>
Correct the inaccurate AUTH-03 completion status in REQUIREMENTS.md and update the Phase 01 summary to reflect the deferral accurately.

Purpose: The v1.0 audit found that AUTH-03 (Apple OAuth) is marked `[x]` complete in REQUIREMENTS.md even though the Apple Developer account was never configured — only the code was wired. This is a documentation inaccuracy that misrepresents v1.0 status.

Output: REQUIREMENTS.md has AUTH-03 as `[ ]` with an inline deferral note. The 01-05-SUMMARY.md no longer lists AUTH-03 in requirements-completed.
</objective>

<execution_context>
@C:/Users/adiln/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adiln/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct AUTH-03 checkbox and add deferral note in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
    In `.planning/REQUIREMENTS.md`, make the following two edits:

    1. Change the AUTH-03 checkbox from `[x]` to `[ ]`:
       - Before: `- [x] **AUTH-03**: User can sign in with Apple OAuth`
       - After:  `- [ ] **AUTH-03**: User can sign in with Apple OAuth _(deferred: code wired, Apple Developer account not configured — post-v1)_`

    2. In the Traceability table, the AUTH-03 row already reads `Phase 1 → Phase 8 (tech debt / deferred) | Deferred` — confirm this is already accurate and leave it unchanged. Do NOT change the `Deferred` status in the traceability table.

    3. Update the Coverage block at the bottom. Change:
       - `- Satisfied: 10 (AUTH-01, AUTH-02, AUTH-04, ...)`
         Remove AUTH-03 is already not in the Satisfied list per the existing file — verify this. If AUTH-03 appears in the Satisfied list, remove it and increment Deferred count from 1 to 1 (already correct).
       - The Deferred line should read: `- Deferred: 1 (AUTH-03 — Apple OAuth provider config deferred post-v1)`

    No other changes to REQUIREMENTS.md. Do not alter any other checkboxes.
  </action>
  <verify>
    <automated>grep -n "AUTH-03" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>
    `grep "AUTH-03" .planning/REQUIREMENTS.md` shows `[ ]` (not `[x]`) and the deferred inline note. The traceability row still says `Deferred`. No other requirement statuses changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove AUTH-03 from requirements-completed in 01-05-SUMMARY.md</name>
  <files>.planning/phases/01-foundation/01-05-SUMMARY.md</files>
  <action>
    In `.planning/phases/01-foundation/01-05-SUMMARY.md`, find the frontmatter line:

    ```
    requirements-completed: [AUTH-01, AUTH-02, AUTH-03, AUTH-04]
    ```

    Change it to:

    ```
    requirements-completed: [AUTH-01, AUTH-02, AUTH-04]
    ```

    Also find the prose section "Issues Encountered" which already documents the Apple OAuth deferral:
    > "Apple OAuth deferred: Apple Developer account not yet configured."

    This note is already present and accurate — leave it unchanged.

    No other edits to the SUMMARY. Do not change any other sections.
  </action>
  <verify>
    <automated>grep -n "requirements-completed\|AUTH-03" .planning/phases/01-foundation/01-05-SUMMARY.md</automated>
  </verify>
  <done>
    `requirements-completed` line no longer includes AUTH-03. AUTH-03 still appears in the "Issues Encountered" prose note explaining the deferral. No other lines changed.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `grep "AUTH-03" .planning/REQUIREMENTS.md` returns the `[ ]` line (not `[x]`)
- `grep "requirements-completed" .planning/phases/01-foundation/01-05-SUMMARY.md` does not contain AUTH-03
- `npx vitest run` result is unaffected by this plan (documentation-only changes)
</verification>

<success_criteria>
AUTH-03 is documented as deferred (not falsely complete) across REQUIREMENTS.md and the Phase 01 summary. Any future audit reading these files will find an accurate status.
</success_criteria>

<output>
After completion, create `.planning/phases/08-resolve-tech-debt/08-01-SUMMARY.md`
</output>
