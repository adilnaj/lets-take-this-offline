---
phase: 08-resolve-tech-debt
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/seed/seed.test.ts
  - tests/seed/embeddings.test.ts
  - tests/seed/uniqueness.test.ts
autonomous: true
requirements: [AUTH-03]

must_haves:
  truths:
    - "npx vitest run exits 0 — no failing tests"
    - "The 4 seed test cases are skipped with vi.skip (not expect(false).toBe(true) stubs)"
    - "Each skipped test has a comment explaining why it is skipped and what it would need to run"
  artifacts:
    - path: "tests/seed/seed.test.ts"
      provides: "Skipped idempotency test with reason comment"
      contains: "it.skip"
    - path: "tests/seed/embeddings.test.ts"
      provides: "Skipped embeddings test with reason comment"
      contains: "it.skip"
    - path: "tests/seed/uniqueness.test.ts"
      provides: "Skipped uniqueness tests with reason comment"
      contains: "it.skip"
  key_links:
    - from: "tests/seed/*.test.ts"
      to: "vitest exit code"
      via: "it.skip causes SKIP status, not FAIL status"
      pattern: "it\\.skip"
---

<objective>
Convert the 4 seed test stubs from `expect(false).toBe(true)` (which cause `npx vitest run` to exit 1) to `it.skip(...)` (which exits 0), with inline comments explaining the skip rationale.

Purpose: The three seed test files contain intentional stubs that force a test failure. This means `npx vitest run` always exits 1, which blocks CI and makes it impossible to distinguish real test failures from known deferred tests. These tests require a live Supabase test database to run — they are integration tests, not unit tests, and that infrastructure is out of scope for v1.0.

Output: All 4 test cases converted to `it.skip` with a one-line reason comment. `npx vitest run` exits 0.
</objective>

<execution_context>
@C:/Users/adiln/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adiln/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@tests/seed/seed.test.ts
@tests/seed/embeddings.test.ts
@tests/seed/uniqueness.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert seed test stubs from expect(false) to it.skip</name>
  <files>tests/seed/seed.test.ts, tests/seed/embeddings.test.ts, tests/seed/uniqueness.test.ts</files>
  <action>
    Convert all 4 test cases across the three files from failing stubs to properly-skipped tests. The pattern for each is:

    **Before (current state — causes exit 1):**
    ```typescript
    it('...description...', async () => {
      // TODO: implement — ...
      expect(false).toBe(true) // stub: replace with real assertions
    })
    ```

    **After (target state — causes exit 0):**
    ```typescript
    it.skip('...description...', async () => {
      // Skipped: requires live Supabase test DB with seeded data.
      // To enable: set up a test Supabase project, run the seed script,
      // and replace this stub with assertions against that DB.
    })
    ```

    Apply this pattern to all 4 test cases:

    **tests/seed/seed.test.ts** — 1 test:
    - `it('re-running the seed script skips existing words by slug', ...)` → `it.skip`
    - Remove the `expect(false).toBe(true)` line and the `// TODO` comment
    - Add: `// Skipped: requires live Supabase test DB. Re-running seed against test DB and asserting row count is unchanged is an integration test deferred post-v1.`

    **tests/seed/embeddings.test.ts** — 1 test:
    - `it('each seeded word has a non-null embedding of length 1024', ...)` → `it.skip`
    - Remove the `expect(false).toBe(true)` line and the `// TODO` comment
    - Add: `// Skipped: requires live Supabase test DB. Asserting embedding dimensions requires querying the words table with a real connection deferred post-v1.`

    **tests/seed/uniqueness.test.ts** — 2 tests:
    - `it('all seeded words have unique daily_date values', ...)` → `it.skip`
    - `it('all seeded words have unique slug values', ...)` → `it.skip`
    - Remove both `expect(false).toBe(true)` stubs and `// TODO` comments
    - Add to each: `// Skipped: requires live Supabase test DB. Uniqueness is enforced by DB UNIQUE constraint — integration test deferred post-v1.`

    Keep the `import { describe, it, expect } from 'vitest'` import line in each file unchanged (even if `expect` is now unused — removing it is unnecessary noise).

    Do NOT use `it.todo()` — that requires no callback at all. Use `it.skip('description', async () => { // comment })` which keeps the test body but marks it as intentionally skipped.
  </action>
  <verify>
    <automated>npx vitest run 2>&1 | tail -10</automated>
  </verify>
  <done>
    `npx vitest run` exits 0. The vitest output shows the 4 seed tests as skipped (e.g. "4 skipped") rather than failed. No test suite reports a failure.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` exits 0
- `grep -r "expect(false)" tests/seed/` returns no matches
- `grep -c "it.skip" tests/seed/uniqueness.test.ts` returns 2
- `grep "it.skip" tests/seed/seed.test.ts` returns 1 match
- `grep "it.skip" tests/seed/embeddings.test.ts` returns 1 match
</verification>

<success_criteria>
`npx vitest run` exits 0. The 4 seed test stubs are properly skipped with explanatory comments. Any engineer running the test suite can distinguish "known-deferred integration tests" from "real failures" at a glance.
</success_criteria>

<output>
After completion, create `.planning/phases/08-resolve-tech-debt/08-03-SUMMARY.md`
</output>
