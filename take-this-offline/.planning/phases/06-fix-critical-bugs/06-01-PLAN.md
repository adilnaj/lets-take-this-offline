---
phase: 06-fix-critical-bugs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/og/[slug]/route.tsx
  - src/app/api/sentence-feedback/route.ts
autonomous: true
requirements:
  - PLAT-05
  - ACTV-03

must_haves:
  truths:
    - "Sharing a word URL on Slack/LinkedIn/X renders a dynamic OG image instead of an error"
    - "The sentence-feedback API completes without a model-not-found error from Anthropic"
  artifacts:
    - path: "src/app/api/og/[slug]/route.tsx"
      provides: "Edge-runtime OG image generation"
      contains: "export const runtime = 'edge'"
    - path: "src/app/api/sentence-feedback/route.ts"
      provides: "AI sentence feedback via Anthropic"
      contains: "claude-haiku-4-5-20251001"
  key_links:
    - from: "src/app/api/og/[slug]/route.tsx"
      to: "next/og ImageResponse"
      via: "Edge runtime"
      pattern: "runtime.*edge"
    - from: "src/app/api/sentence-feedback/route.ts"
      to: "anthropic.messages.create"
      via: "FEEDBACK_MODEL constant"
      pattern: "claude-haiku-4-5-20251001"
---

<objective>
Fix two production bugs identified by the v1.0 audit that cause hard failures.

Purpose: OG images fail on the Vercel Edge network without `export const runtime = 'edge'` because `ImageResponse` from `next/og` requires the Edge runtime. The sentence-feedback route uses an invalid Anthropic model ID (`claude-haiku-3-5`) that returns a model-not-found error on every call.

Output: Two corrected route files — OG route with the required runtime export, sentence-feedback route with the correct model ID.
</objective>

<execution_context>
@C:/Users/adiln/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adiln/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Edge runtime export to OG image route</name>
  <files>src/app/api/og/[slug]/route.tsx</files>
  <action>
    Add `export const runtime = 'edge'` as the second line of the file, immediately after the imports block and before the GET function.

    The file currently has no runtime export. `ImageResponse` from `next/og` requires the Edge runtime to execute on Vercel — without it, the route runs in the Node.js runtime and `ImageResponse` fails to serialize in production.

    Current file structure:
    ```
    import { ImageResponse } from 'next/og'
    import { NextRequest } from 'next/server'
    import { getWordBySlug } from '@/lib/words'

    export async function GET(...) { ... }
    ```

    After the change:
    ```
    import { ImageResponse } from 'next/og'
    import { NextRequest } from 'next/server'
    import { getWordBySlug } from '@/lib/words'

    export const runtime = 'edge'

    export async function GET(...) { ... }
    ```

    Do not change any other code in the file.
  </action>
  <verify>
    <automated>grep -n "export const runtime" src/app/api/og/\[slug\]/route.tsx</automated>
  </verify>
  <done>`export const runtime = 'edge'` appears in the file between the imports and the GET function.</done>
</task>

<task type="auto">
  <name>Task 2: Fix invalid Anthropic model ID in sentence-feedback route</name>
  <files>src/app/api/sentence-feedback/route.ts</files>
  <action>
    Change the `FEEDBACK_MODEL` constant value from `'claude-haiku-3-5'` to `'claude-haiku-4-5-20251001'`.

    Current line (line 5):
    ```typescript
    const FEEDBACK_MODEL = 'claude-haiku-3-5'
    ```

    Replace with:
    ```typescript
    const FEEDBACK_MODEL = 'claude-haiku-4-5-20251001'
    ```

    The model ID `claude-haiku-3-5` is not a valid Anthropic API model identifier. The correct versioned ID is `claude-haiku-4-5-20251001`. This constant is used only in the `anthropic.messages.create` call on line 20 — no other changes are needed.

    Do not change any other code in the file. Note in the STATE.md decisions log: "FEEDBACK_MODEL corrected to claude-haiku-4-5-20251001 (was invalid 'claude-haiku-3-5')".
  </action>
  <verify>
    <automated>grep -n "FEEDBACK_MODEL" src/app/api/sentence-feedback/route.ts</automated>
  </verify>
  <done>`FEEDBACK_MODEL` is set to `'claude-haiku-4-5-20251001'` and the old value `'claude-haiku-3-5'` no longer appears in the file.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep "export const runtime" src/app/api/og/\[slug\]/route.tsx` outputs `export const runtime = 'edge'`
2. `grep "FEEDBACK_MODEL" src/app/api/sentence-feedback/route.ts` shows `claude-haiku-4-5-20251001`
3. `npx tsc --noEmit` passes with no new type errors
</verification>

<success_criteria>
- OG route contains `export const runtime = 'edge'` between the imports and GET export
- sentence-feedback route contains `'claude-haiku-4-5-20251001'` as the model ID
- Neither file has any other modifications
- TypeScript compilation succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/06-fix-critical-bugs/06-01-SUMMARY.md`
</output>
