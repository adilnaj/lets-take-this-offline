import { test } from '@playwright/test'

test.describe('Favorites (GAME-04)', () => {
  test.fixme('favorite button is visible on word page for logged-in user', async () => {})
  test.fixme('clicking favorite button shows filled heart icon', async () => {})
  test.fixme('favorited word appears in /profile favorites section', async () => {})
  test.fixme('clicking favorite button again removes it from /profile', async () => {})
})
