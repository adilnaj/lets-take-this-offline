import { test } from '@playwright/test'

test.describe('Activity History (GAME-05)', () => {
  test.fixme('/profile page requires login — redirects anonymous user to /auth/sign-in', async () => {})
  test.fixme('/profile shows list of past words the user practiced with mastery levels', async () => {})
  test.fixme('/profile shows mastery rating badge (seen/learning/mastered) per word', async () => {})
  test.fixme('/profile shows total points and streak counter with flame icon', async () => {})
})
